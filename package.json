{
  "dependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  },
  "devDependencies": {
    "@types/jest": "^29.5.3",
    "jest": "^29.6.1",
    "ts-jest": "^29.1.1"
  },
  "scripts": {
    "test": "jest",
    "test:cov": "yarn jest --coverage",
    "test:cov-ci-json": "jest --ci --json --coverage --testLocationInResults --outputFile=report.json"
  }
}
